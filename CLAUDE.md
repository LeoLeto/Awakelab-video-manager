# awakelab-video-manager — Claude preferences

## Version bumping
Every change to the client app must increment `APP_VERSION` in `client/src/App.tsx`.
Use patch increments (e.g. 3.1 → 3.2) for fixes and small features; minor increments (3.x → 4.0) for significant redesigns.
